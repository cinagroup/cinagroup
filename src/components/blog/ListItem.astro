---
import type { ImageMetadata } from 'astro';
import Image from '~/components/common/Image.astro';
import PostTags from '~/components/blog/Tags.astro';

import { APP_BLOG } from 'astrowind:config';
import type { Post } from '~/types';

import { getPermalink } from '~/utils/permalinks';
import { findImage } from '~/utils/images';
import { getFormattedDate } from '~/utils/utils';

export interface Props {
  post: Post;
}

const { post } = Astro.props;
const image = (await findImage(post.image)) as ImageMetadata | undefined;

const link = APP_BLOG?.post?.isEnabled ? getPermalink(post.permalink, 'post') : '';
const isAutomatedBriefing = post.slug.startsWith('ai-news-briefing-');
---

<article
  class:list={['cg-blog-item', { 'cg-blog-item-with-image': image }]}
>
  {
    image &&
      (link ? (
        <a class="cg-blog-image" href={link}>
          <div>
            {image && (
              <Image
                src={image}
                class="w-full h-full object-cover"
                widths={[400, 900]}
                width={900}
                sizes="(max-width: 900px) 400px, 900px"
                alt={post.title}
                aspectRatio="16:9"
                loading="lazy"
                decoding="async"
              />
            )}
          </div>
        </a>
      ) : (
        <div class="cg-blog-image">
          {image && (
            <Image
              src={image}
              class="w-full h-full object-cover"
              widths={[400, 900]}
              width={900}
              sizes="(max-width: 900px) 400px, 900px"
              alt={post.title}
              aspectRatio="16:9"
              loading="lazy"
              decoding="async"
            />
          )}
        </div>
      ))
  }
  <div class="cg-blog-item-copy">
    <header>
      <div class="cg-blog-meta">
        <span>
          {isAutomatedBriefing && <><span class="cg-blog-kind">Automated briefing</span>{' '}·{' '}</>}
          <time datetime={String(post.publishDate)} class="inline-block">{getFormattedDate(post.publishDate)}</time>
          {
            post.author && (
              <>
                {' '}·{' '}
                <span>{post.author.replaceAll('-', ' ')}</span>
              </>
            )
          }
          {
            post.category && (
              <>
                {' '}·{' '}
                <a href={getPermalink(post.category.slug, 'category')}>
                  {post.category.title}
                </a>
              </>
            )
          }
        </span>
      </div>
      <h2>
        {
          link ? (
            <a
              class="cg-blog-title-link"
              href={link}
            >
              {post.title}
            </a>
          ) : (
            post.title
          )
        }
      </h2>
    </header>

    {post.excerpt && <p>{post.excerpt}</p>}
    {
      post.tags && Array.isArray(post.tags) ? (
        <footer class="cg-blog-tags">
          <PostTags tags={post.tags} />
        </footer>
      ) : (
        <Fragment />
      )
    }
  </div>
</article>
